import { StrategyInterface } from '../model';
/**
 * Calculates the new table configuration while the tiles are moving
 * @param offsetX
 * @param offsetY
 * @returns
 */
declare const dragMove: StrategyInterface;
export default dragMove;
